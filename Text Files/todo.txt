function call without semi in the end
 int x = add(3,5) + 4;

 mawdoo3 unary ops 
    minus kaza


Switch case issue in mult defaults
Support  int char bool only 


Return only works lie return; but not return expression; like return 5; or return "adas";




restrict func def inside scopes make it only global


for loop iterator , cond , step
w 7owar el same identifier 3ashan n avoid el inf loops