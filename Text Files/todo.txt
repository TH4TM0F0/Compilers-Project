SEMANTICS:
    Types int float bool mostly can be converted (ana elha3melo fel symbol table msh boda)




int and float issue after assigning diff types (need to cast or handle it through the union)

inc w dec don't update the current value in the symbol table

assigning expressions to variables

negative nums are not stored ?????????




isUsed flag for functions when func call
for variables also


func decl working fine 

call functions with no args even it has params



